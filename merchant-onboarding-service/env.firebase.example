# Firebase Storage Configuration
# These variables are required for merchant profile image uploads to Firebase Storage

# Your Firebase project ID (found in Firebase Console → Project Settings → General)
FIREBASE_PROJECT_ID=your-project-id

# Service account email (found in the downloaded JSON file as "client_email")
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# Private key from the service account JSON file (found as "private_key")
# IMPORTANT: 
# - Wrap the entire value in double quotes
# - Keep the \n characters (they represent line breaks)
# - Include the BEGIN and END lines
# - Example format:
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Instructions:
# 1. Go to Firebase Console (https://console.firebase.google.com/)
# 2. Select your project → Project Settings → Service Accounts
# 3. Click "Generate new private key" to download JSON file
# 4. Extract the three values above from the JSON file
# 5. Copy this file to .env and fill in the values
# 6. See FIREBASE_STORAGE_SETUP.md for detailed instructions

